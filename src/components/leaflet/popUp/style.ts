const popupContent: { [key: string]: string } = {
    textAlign: 'center',
    height: '200px',
    marginTop: '30px',
    display: 'flex',
}
const popupHead: { [key: string]: string } = {
    fontWeight: 'bold',
    fontSize: '22px',
}

const popupText: { [key: string]: string } = {
    fontSize: '15px',
    marginBottom: '20px',
}

const okText: { [key: string]: string } = {
    fontSize: '15px',
}

export { popupContent, popupHead, popupText, okText }
